# Task 7 report — dynamic collector/import contract

## Characterization evidence

The fixture was generated by `collector.collect_fish_images.write_manifest()`
with two current-format rows: dynamic species codes `FISH_A` and `FISH_B`,
sources `INATURALIST` and `GBIF`, licenses `CC-BY` and `CC0`, public HTTPS
source/image URLs, and blank local/review fields. The writer emitted a UTF-8
BOM.

The required focused run passed immediately, so this is characterization
evidence and no artificial RED was created:

```text
python -m pytest tests/test_collector_import_contract.py -q  # from api/
1 passed in 0.47s
```

The real empty-candidate SQLite test database replaced its initial species
with `FISH_A` and `FISH_B`; Mao staged the generated bytes and committed them.
The asserted preview counts were `total=2`, `new_rows=2`, and
`blocking_errors=0`; the commit result was `inserted=2`. No external HTTP
request was made. No current collector manifest correction or ZIP regeneration
was warranted.

## Normal verification matrix

- `python -m pytest collector/tests -q`: `28 passed in 1.01s`.
- `python -m compileall -q collector`: passed.
- `python collector/build_package.py --check`: passed, including the final
  artifact check.
- `python -m pytest -q` from `api/`: `414 passed, 30 skipped in 159.90s`.
- `python -m compileall -q app tests alembic`: passed.
- Alembic used only the brand-new temporary SQLite file
  `C:\Users\86166\Desktop\sukaSeafoodReview\.worktrees\collaborative-review\api\.task7-alembic-check.sqlite3`.
  Upgrade ran revisions `20260826_01` through `20260827_07`; `alembic check`
  reported `No new upgrade operations detected.` The 253,952-byte temporary
  file was removed and confirmed absent.
- The first `npm test` run from `web/` exposed one stale integration assertion:
  `src/App.integration.test.tsx:170` still expected `导入` after Task 5 renamed
  the tab to `采集与导入` (`210 passed, 1 failed`). The original Task 5
  implementer corrected only that test expectation in commit `1f9fb29`; the
  isolated file then passed `5/5` and the complete web suite passed `211/211`.
- `npm run typecheck`: passed.
- `npm run build`: passed; Vite transformed 49 modules and built in `1.09s`.
- `python -m pytest tests -q`: `21 passed, 11 skipped in 2.09s`.
- `git diff --check`: passed.
- Before committing, `git status --short` contained only the two required new
  acceptance artifacts; this report was then added for the required handoff.
  Exact post-commit SHA and status are reported to the controller.

## Commit

`test: verify dynamic collector import flow`

## Artifact and deletion preconditions

All required migrated artifacts exist as regular files:

- `collector/collect_fish_images.py`
- `collector/species_config.example.json`
- `collector/requirements.txt`
- `collector/README_ZH.md`
- `collector/README.md`
- `web/public/downloads/sukaseafood-collector.zip`

After the repaired complete web suite passed, the controller independently
confirmed a clean Git worktree, a fresh ZIP, all six migrated artifacts, and
that `C:\Users\86166\Desktop\SukaSeafood_CV_Dataset_Collector` resolved to the
exact explicitly authorized real directory and was not a reparse point. The
controller then deleted only that directory and confirmed it no longer exists.
The old 1,221-row CSV, cached thumbnails, and legacy output were intentionally
removed; the migrated source, documentation, and downloadable ZIP remain in the
committed repository.

## Concerns

None. The collector-to-importer path required no runtime correction, the normal
matrix is green after the narrow stale-test repair, and the authorized legacy
directory removal is complete. No compatibility, concurrency, crash, upgrade,
network, deployment, push, merge, or production work was added or run.
